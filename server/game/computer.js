// Not implemented yet. When built, the bot should compute a move object in the same
// { type, ...payload } shape a human client sends (see server/game/rules.js) and feed
// it through game.makeMove(botPlayerId, move) -- no special-cased AI path elsewhere.
